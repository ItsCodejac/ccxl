---
phase: 06-config-maintenance
plan: 01
type: execute
---

<objective>
Create the update command that detects drift between current codebase state and generated configs, and suggests/applies updates when the project changes (new deps, new frameworks, etc.).

Purpose: Projects evolve — new dependencies get added, frameworks change, CI/CD gets set up. ccxl should detect these changes and update configs accordingly.
Output: `ccxl update` command that re-scans and diffs against existing configs.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/analyzer/analyze.ts
@src/generators/pipeline.ts
@src/generators/merge.ts
@src/commands/update.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement drift detection and update command</name>
  <files>src/maintenance/drift.ts, src/maintenance/index.ts, src/commands/update.ts</files>
  <action>
  1. Create src/maintenance/drift.ts:
     - async function detectDrift(root: string): Promise<DriftReport>
     - Re-runs analyzeProject on current state
     - Re-runs generator pipeline (dry-run mode)
     - Compares generated files against existing files:
       a. New files that don't exist yet → "missing" (ccxl has new suggestions)
       b. Files that exist but differ from generated → "stale" (project changed, config didn't)
       c. Files that match → "current" (no action needed)
     - DriftReport: { missing: GeneratedFile[], stale: GeneratedFile[], current: string[] }

  2. Create src/maintenance/index.ts with exports.

  3. Update src/commands/update.ts:
     - `ccxl update` — runs drift detection, shows report
     - Shows: "X configs are current, Y are stale, Z new suggestions"
     - For stale/missing: shows what would change
     - `ccxl update --apply` — applies stale and missing files using merge logic
     - `ccxl update --dry-run` — show report only (default behavior without --apply)

  4. Simple string comparison for drift — if generated content differs from existing file, it's stale. Don't do semantic diffing.
  </action>
  <verify>npm run build succeeds, node dist/cli.js update works (shows drift report for ccxl project itself)</verify>
  <done>Update command detects config drift and suggests/applies updates</done>
</task>

<task type="auto">
  <name>Task 2: Implement doctor command with diagnostics</name>
  <files>src/maintenance/diagnostics.ts, src/commands/doctor.ts, src/maintenance/__tests__/maintenance.test.ts</files>
  <action>
  1. Create src/maintenance/diagnostics.ts:
     - async function runDiagnostics(root: string): Promise<DiagnosticResult[]>
     - DiagnosticResult: { name, status: 'pass'|'warn'|'fail', message }
     - Checks:
       a. .claude/settings.json exists and is valid JSON
       b. .claude/settings.json has permissions defined
       c. CLAUDE.md exists
       d. No .env files are readable (deny rules working)
       e. Skills directory has files
       f. Hook scripts are executable (chmod +x)
       g. ccxl-packages.json is valid if present
       h. No conflicting configs (e.g., both .cursorrules and .cursor/rules/)
     - `--fix` flag auto-fixes: set chmod +x on scripts, create missing dirs

  2. Update src/commands/doctor.ts:
     - Replace stub with actual implementation
     - `ccxl doctor` — runs all diagnostics, prints pass/warn/fail for each
     - `ccxl doctor --fix` — auto-fix where possible
     - `ccxl doctor --verbose` — show details for each check

  3. Tests:
     - Drift detection: project with changed deps returns stale files
     - Diagnostics: valid setup passes all checks
     - Diagnostics: missing settings.json reports fail
     - Diagnostics: non-executable scripts report warn (with --fix suggestion)
  </action>
  <verify>npm run test:run passes, npm run build succeeds, node dist/cli.js doctor works</verify>
  <done>Doctor command runs 8 diagnostic checks with pass/warn/fail reporting and auto-fix. Phase 6 complete.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:run` passes
- [ ] `npm run build` succeeds
- [ ] `node dist/cli.js update` shows drift report
- [ ] `node dist/cli.js doctor` shows diagnostic results
- [ ] Phase 6 complete
</verification>

<success_criteria>
- Update command detects drift and applies changes
- Doctor command runs diagnostics with auto-fix
- Phase 6: Config Maintenance is complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-config-maintenance/06-01-SUMMARY.md`
</output>
