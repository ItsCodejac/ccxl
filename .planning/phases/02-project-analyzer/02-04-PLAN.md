---
phase: 02-project-analyzer
plan: 04
type: execute
---

<objective>
Implement existing AI tool config detection and wire up the complete analyzer to the init command, producing a visible scan result in the TUI.

Purpose: This is the final piece of the analyzer — detecting what's already configured, then proving the entire pipeline works by rendering results in the CLI.
Output: Working `ccxl init` that scans the current project and displays analysis results.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-project-analyzer/02-01-SUMMARY.md
@.planning/phases/02-project-analyzer/02-02-SUMMARY.md
@.planning/phases/02-project-analyzer/02-03-SUMMARY.md
@src/analyzer/analyze.ts
@src/analyzer/detector.ts
@src/commands/init.ts
@src/tui/views/InitView.tsx

**Tech stack available:** TypeScript 5.5, vitest 2, zod 3, fs-extra 11, ink 5, react 18
**Established patterns:** Detector interface, mock fs tests, dynamic import for views, per-command modules
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create existing config detector</name>
  <files>src/analyzer/detectors/existing-config.ts, src/analyzer/detectors/__tests__/existing-config.test.ts</files>
  <action>
  Implement ExistingConfigDetector that checks for AI tool configs:

  Checks:
  - .claude/settings.json → Claude Code (parse for version hints, list features found: skills, hooks, agents, mcp)
  - CLAUDE.md → Claude Code CLAUDE.md (check if exists, estimate quality: byte count, has sections)
  - .claude/skills/ → Claude Code skills directory (count files)
  - .cursorrules → Cursor (check exists, read byte count)
  - .cursor/rules/ → Cursor rules directory
  - copilot-instructions.md or .github/copilot-instructions.md → GitHub Copilot
  - .windsurfrules → Windsurf
  - .mcp.json → MCP config (parse and list configured servers)
  - AGENTS.md → Agent definitions

  Return ExistingConfigInfo[] with tool name, path, exists flag, and discovered features.

  Write tests with mock file systems that verify detection for each tool config.

  Register detector in orchestrator.
  </action>
  <verify>npm run test:run passes existing config tests, npx tsc --noEmit passes</verify>
  <done>Existing config detector finds all AI tool configs, reports features discovered in each</done>
</task>

<task type="auto">
  <name>Task 2: Wire analyzer to init command with TUI output</name>
  <files>src/commands/init.ts, src/tui/views/InitView.tsx, src/tui/views/AnalysisView.tsx</files>
  <action>
  Connect the full analyzer pipeline to the init command:

  1. Create src/tui/views/AnalysisView.tsx:
     - Receives ProjectAnalysis result
     - Displays: detected languages, frameworks, package manager, monorepo info
     - Displays: CI/CD, cloud providers, databases, Docker status
     - Displays: existing AI tool configs with feature counts
     - Uses theme colors for visual hierarchy
     - Groups results into sections with clear headings
     - Shows "No X detected" for empty categories (don't hide them)

  2. Update src/commands/init.ts:
     - After user selects setup type, run analyzeProject(process.cwd())
     - Show Spinner while analyzing ("Scanning project...")
     - Render AnalysisView with results
     - For --yes flag: skip TUI, print summary to stdout using chalk
     - For --dry-run: show analysis only, don't proceed to generation

  3. Update InitView.tsx:
     - After selection, transition to analysis phase
     - Show spinner → analysis results → (future: generation options)

  This proves the full pipeline: CLI command → analyzer → TUI rendering.
  </action>
  <verify>npm run build succeeds, node dist/cli.js init --yes scans current project and prints analysis to stdout, node dist/cli.js init shows TUI with analysis results</verify>
  <done>ccxl init scans project, displays all detected languages/frameworks/infra/configs in TUI, --yes mode prints to stdout, Phase 2 complete</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:run` passes all tests
- [ ] `npm run build` succeeds
- [ ] `node dist/cli.js init --yes` scans and prints analysis results
- [ ] All detector types registered in orchestrator
- [ ] Phase 2 complete — all 4 plans done
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Full analyzer pipeline works end-to-end (CLI → detectors → TUI)
- Phase 2: Project Analyzer is complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-analyzer/02-04-SUMMARY.md`
</output>
