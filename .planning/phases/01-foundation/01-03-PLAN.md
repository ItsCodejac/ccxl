---
phase: 01-foundation
plan: 03
type: execute
---

<objective>
Build the Ink TUI scaffold — reusable terminal UI components that all future commands will use for interactive setup, previews, progress display, and config browsing.

Purpose: ccxl's TUI is core to its UX. Every command (init, generate, registry browse, doctor) uses these components. Building them now means future phases just compose existing UI.
Output: Library of Ink components ready for integration with CLI commands.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@src/cli.ts
@src/tui/index.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create core Ink components — App shell, Header, StatusBar, Spinner</name>
  <files>src/tui/App.tsx, src/tui/components/Header.tsx, src/tui/components/StatusBar.tsx, src/tui/components/Spinner.tsx, src/tui/theme.ts, src/tui/index.ts</files>
  <action>
  Set up the Ink component library:

  1. Create src/tui/theme.ts:
     - Define color palette (brand colors for ccxl — use cyan/blue tones)
     - Export theme object with: primary, secondary, success, warning, error, muted, border colors
     - Export text style helpers (bold, dim, accent)

  2. Create src/tui/components/Header.tsx:
     - Displays "ccxl" branding with version
     - Shows current command name
     - Uses Box with borderStyle for framing

  3. Create src/tui/components/StatusBar.tsx:
     - Bottom bar showing current step/phase
     - Progress indicator (step X of Y)
     - Optional elapsed time

  4. Create src/tui/components/Spinner.tsx:
     - Wraps ink-spinner with consistent styling
     - Takes label prop
     - Uses theme colors

  5. Create src/tui/App.tsx:
     - Shell component that wraps any command's UI
     - Renders Header at top, children in middle, StatusBar at bottom
     - Accepts title, step, totalSteps props

  6. Export everything from src/tui/index.ts

  Use .tsx extension for all React/Ink components. Ensure tsconfig includes jsx: "react-jsx" and tsup handles tsx files.
  </action>
  <verify>npm run build succeeds with tsx components, no TypeScript errors, all components exported from src/tui/index.ts</verify>
  <done>Theme system works, Header/StatusBar/Spinner components exist and compile, App shell wraps content with consistent layout</done>
</task>

<task type="auto">
  <name>Task 2: Create interactive components — SelectPrompt, ConfirmPrompt, PreviewPanel</name>
  <files>src/tui/components/SelectPrompt.tsx, src/tui/components/ConfirmPrompt.tsx, src/tui/components/PreviewPanel.tsx, src/tui/components/FileList.tsx, src/tui/index.ts</files>
  <action>
  Build interactive UI components:

  1. Create src/tui/components/SelectPrompt.tsx:
     - Multi-option selector using Ink's useInput hook
     - Arrow key navigation with highlighted current option
     - Optional descriptions per option
     - Returns selected value via onSelect callback
     - Supports multi-select mode (checkbox style)

  2. Create src/tui/components/ConfirmPrompt.tsx:
     - Simple yes/no prompt with keyboard input
     - Customizable question text
     - Default value support
     - Returns boolean via onConfirm callback

  3. Create src/tui/components/PreviewPanel.tsx:
     - Side-by-side or stacked preview of generated config
     - Syntax-highlighted file content (use chalk for coloring)
     - Shows file path, diff indicators (new/modified)
     - Scrollable if content exceeds terminal height

  4. Create src/tui/components/FileList.tsx:
     - Shows list of files that will be created/modified
     - Status icons: + (new), ~ (modified), - (removed)
     - Grouped by directory
     - Used in dry-run and preview modes

  5. Update src/tui/index.ts to export all new components.

  6. Wire up one command to demo the TUI — update the `init` command stub to render the App shell with a Header showing "ccxl init" and a simple SelectPrompt asking "What would you like to configure?" with options like "Full setup", "Skills only", "Hooks only". This proves the TUI pipeline works end-to-end from CLI → Ink render.
  </action>
  <verify>npm run build succeeds, node dist/cli.js init renders the Ink TUI with header and select prompt, arrow keys navigate options, selecting an option prints the selection</verify>
  <done>All interactive components compile and export, init command renders working TUI, keyboard navigation works, PreviewPanel and FileList ready for future use</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds with all .tsx components
- [ ] `node dist/cli.js init` renders Ink TUI with Header, SelectPrompt
- [ ] Arrow key navigation works in SelectPrompt
- [ ] All components exported from `src/tui/index.ts`
- [ ] Theme colors applied consistently
- [ ] No TypeScript errors
- [ ] Phase 1 complete — all 3 plans done
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- TUI components are reusable and composable
- CLI renders Ink UI for at least one command
- Phase 1: Foundation is complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
