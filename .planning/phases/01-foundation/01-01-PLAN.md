---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up the TypeScript project from scratch — rewrite package.json, configure TypeScript compilation, set up tsup for fast builds, and establish the src/ directory structure for all future phases.

Purpose: Every subsequent phase depends on a working TypeScript build pipeline. This is the foundation everything else builds on.
Output: Clean TypeScript project that compiles, with all old JavaScript source removed from the build path.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite package.json and install TypeScript toolchain</name>
  <files>package.json, tsconfig.json</files>
  <action>
  Rewrite package.json:
  - Keep: name (ccxl), version (bump to 3.0.0-alpha.0), description (update), bin, engines, repository, author, license, keywords (expand)
  - Remove all old dependencies (chalk, inquirer, ora, markdown-it, fs-extra)
  - Add dependencies: ink (^5), react (^18), commander (^12), zod (^3), chalk (^5), fs-extra (^11), ink-spinner (latest)
  - Add devDependencies: typescript (^5.5), @types/node (^20), @types/react (^18), @types/fs-extra (^11), tsup (^8), vitest (^2), @ink/testing-library (latest), eslint (^9), @typescript-eslint/eslint-plugin, @typescript-eslint/parser, tsx (^4)
  - Set "type": "module"
  - Set main: "dist/index.js", types: "dist/index.d.ts"
  - Update bin to: { "ccxl": "./dist/cli.js" }
  - Update files to: ["dist/"]
  - Scripts: "build": "tsup", "dev": "tsx src/cli.ts", "test": "vitest", "test:run": "vitest run", "lint": "eslint src/", "prepublishOnly": "npm run build"

  Create tsconfig.json:
  - target: ES2022, module: NodeNext, moduleResolution: NodeNext
  - strict: true, esModuleInterop: true, skipLibCheck: true
  - outDir: "dist", rootDir: "src", declaration: true
  - include: ["src/**/*"]

  Run npm install after writing both files.
  </action>
  <verify>npm install completes without errors, npx tsc --noEmit succeeds (may need src/ files first)</verify>
  <done>package.json has all new deps, tsconfig.json exists, node_modules populated with TypeScript toolchain</done>
</task>

<task type="auto">
  <name>Task 2: Create src/ directory structure and build config</name>
  <files>src/index.ts, src/cli.ts, tsup.config.ts, .eslintrc.cjs</files>
  <action>
  Create directory structure:
  - src/cli.ts — placeholder entry (just exports version from package.json)
  - src/index.ts — placeholder module entry (export { version })
  - src/types/ — empty dir with index.ts exporting placeholder types
  - src/analyzer/ — empty dir with index.ts
  - src/generators/ — empty dir with index.ts
  - src/registry/ — empty dir with index.ts
  - src/tui/ — empty dir with index.ts
  - src/utils/ — empty dir with index.ts

  Create tsup.config.ts:
  - entry: ["src/cli.ts", "src/index.ts"]
  - format: ["esm"]
  - dts: true
  - clean: true
  - target: "node20"
  - banner for cli.ts: "#!/usr/bin/env node"

  Create basic ESLint flat config (eslint.config.js):
  - TypeScript parser and plugin
  - Recommended rules
  - Ignore dist/ and node_modules/

  Verify the build works end-to-end: npm run build should produce dist/cli.js and dist/index.js
  </action>
  <verify>npm run build succeeds, dist/cli.js exists with shebang, dist/index.js exists, npx tsc --noEmit passes</verify>
  <done>TypeScript compiles, tsup produces dist/ output, ESLint config exists, all src/ subdirectories created</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npx tsc --noEmit` passes
- [ ] `dist/cli.js` exists with `#!/usr/bin/env node` shebang
- [ ] `dist/index.js` exists with type declarations
- [ ] All src/ subdirectories exist (analyzer, generators, registry, tui, utils, types)
- [ ] `npm run lint` passes (or has only warnings)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- TypeScript build pipeline works end-to-end
- Project structure ready for Phase 1 Plans 02 and 03
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
