---
phase: 03-claude-code-generator
plan: 04
type: execute
---

<objective>
Create the agents and MCP generators that produce AGENTS.md with project-appropriate subagents and .mcp.json with relevant MCP servers based on detected infrastructure.

Purpose: Custom subagents and MCP servers unlock Claude Code's most powerful features but require specific knowledge of frontmatter fields and server configs. ccxl generates both tuned to the project.
Output: AGENTS.md generator and .mcp.json generator.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-claude-code-generator/03-01-SUMMARY.md
@src/generators/generator.ts
@src/types/analysis.ts

**AGENTS.md format (from research):**
```yaml
---
name: code-reviewer
description: Reviews code for quality
tools: "Read, Grep, Glob"
model: "sonnet"
---
Instructions body
```

**.mcp.json format (from research):**
```json
{
  "server-name": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@scope/server-name"],
    "env": { "KEY": "$ENV_VAR" }
  }
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agents generator with project-appropriate subagents</name>
  <files>src/generators/agents.ts, src/generators/agent-templates.ts, src/generators/__tests__/agents.test.ts</files>
  <action>
  1. Create src/generators/agent-templates.ts:
     Agent templates with conditions:

     Universal agents:
     - code-reviewer: Read-only agent using sonnet, reviews for quality/security/patterns
     - explorer: Fast research agent with Read/Grep/Glob only, uses haiku

     Conditional agents:
     - test-runner: If tests detected, runs and debugs tests (tools: Read, Grep, Bash)
     - db-specialist: If database detected, specialized for schema/query work
     - frontend-dev: If React/Vue/Angular detected, UI component specialist
     - api-developer: If Express/Fastify/backend detected, API endpoint specialist
     - devops: If Docker/CI detected, infrastructure specialist

     Each template returns { name, description, tools, model, body } and a condition function.

  2. Create src/generators/agents.ts implementing Generator interface:
     - Generates .claude/agents/{name}.md for each matching agent template
     - Each file has proper YAML frontmatter + instruction body

  3. Tests:
     - All projects get code-reviewer and explorer
     - React project gets frontend-dev
     - Express project gets api-developer
     - Docker project gets devops
     - Verify frontmatter has required fields (name, description)
     - Verify model field is valid (sonnet, haiku, opus)
  </action>
  <verify>npm run test:run passes, npx tsc --noEmit passes</verify>
  <done>Agent templates produce .claude/agents/*.md with correct frontmatter for detected project type</done>
</task>

<task type="auto">
  <name>Task 2: Create MCP config generator</name>
  <files>src/generators/mcp.ts, src/generators/__tests__/mcp.test.ts</files>
  <action>
  1. Create src/generators/mcp.ts implementing Generator interface:
     Generates .mcp.json based on detected infrastructure:

     Conditional MCP servers:
     - PostgreSQL detected → @modelcontextprotocol/server-postgres (env: DATABASE_URL placeholder)
     - SQLite detected → @modelcontextprotocol/server-sqlite
     - GitHub Actions detected → @modelcontextprotocol/server-github (env: GITHUB_TOKEN)
     - MongoDB detected → suggest community MongoDB MCP
     - Redis detected → suggest community Redis MCP

     Always suggest:
     - context7 for documentation fetching (if not already configured)

     Format: { "server-name": { type: "stdio", command: "npx", args: ["-y", "@scope/pkg"], env: { ... } } }

     If no detectable MCP servers, skip generation (don't create empty .mcp.json).

  2. Tests:
     - Project with PostgreSQL → .mcp.json includes postgres server
     - Project with GitHub Actions → .mcp.json includes github server
     - Project with no databases/CI → no .mcp.json generated
     - Verify env vars use $PLACEHOLDER format
     - Verify command uses npx -y pattern

  Register both generators in pipeline.
  </action>
  <verify>npm run test:run passes</verify>
  <done>MCP generator produces .mcp.json with project-appropriate servers, env var placeholders</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:run` passes
- [ ] `npm run build` succeeds
- [ ] AGENTS.md files have valid frontmatter
- [ ] .mcp.json has valid server definitions
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 5-7 agent templates, 5+ MCP server mappings
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-code-generator/03-04-SUMMARY.md`
</output>
