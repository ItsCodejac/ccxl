---
phase: 03.1-critical-improvements
plan: 03
type: execute
---

<objective>
Add --global flag support so ccxl can install base configs to ~/.claude/ that apply across all projects, not just the current one.

Purpose: Developers want universal skills (review-code, explain-code), global agents (explorer), and base safety hooks available in every project. Global install provides this without per-project setup.
Output: --global flag on init and generate commands writing to ~/.claude/.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/generators/pipeline.ts
@src/commands/init.ts
@src/commands/generate.ts

**Claude Code global config location:** ~/.claude/
- ~/.claude/settings.json — global settings
- ~/.claude/skills/ — global skills
- ~/.claude/agents/ — global agents
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --global flag and target path logic</name>
  <files>src/commands/init.ts, src/commands/generate.ts, src/generators/pipeline.ts</files>
  <action>
  1. Update pipeline.ts:
     - Add `scope: 'project' | 'global'` parameter to runPipeline
     - When scope is 'global':
       a. Use os.homedir() + '/.claude/' as base path instead of project root
       b. Only generate universal configs (skip project-specific conditionals)
       c. CLAUDE.md goes to ~/.claude/CLAUDE.md (global instructions)
       d. Settings go to ~/.claude/settings.json
     - Add a function `getTargetRoot(scope)` that returns the appropriate path

  2. Update init command:
     - Add --global flag: .option('-g, --global', 'Install to ~/.claude/ for all projects')
     - When --global: skip project analysis (no project-specific detection)
     - Generate only universal skills/hooks/agents
     - Print "Installing global config to ~/.claude/"

  3. Update generate command:
     - Add --global flag to generate subcommands
     - Same behavior: write to ~/.claude/ instead of .claude/

  4. For global mode, create a minimal "global analysis" that has no languages/frameworks but triggers universal templates only.
  </action>
  <verify>npm run build succeeds, node dist/cli.js init --global --dry-run shows files targeting ~/.claude/</verify>
  <done>--global flag writes universal configs to ~/.claude/, works on init and generate commands</done>
</task>

<task type="auto">
  <name>Task 2: Test global install behavior</name>
  <files>src/generators/__tests__/pipeline.test.ts</files>
  <action>
  Write tests:
  - Global mode generates only universal skills (run-tests, review-code, generate-tests, explain-code)
  - Global mode does NOT generate conditional skills (deploy, db-migrate, lint-fix)
  - Global mode generates safety hooks but not auto-format
  - Global mode generates code-reviewer and explorer agents only
  - Global mode does not generate .mcp.json (project-specific)
  - File paths use ~/.claude/ prefix for global scope
  </action>
  <verify>npm run test:run passes</verify>
  <done>Global install produces correct subset of universal configs, Phase 3.1 complete</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:run` passes
- [ ] `npm run build` succeeds
- [ ] `node dist/cli.js init --global --dry-run` shows universal configs targeting ~/.claude/
- [ ] Phase 3.1 complete — all 3 plans done
</verification>

<success_criteria>
- --global flag works on init and generate
- Only universal configs generated in global mode
- Files target ~/.claude/ path
- Phase 3.1: Critical Improvements complete
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-critical-improvements/03.1-03-SUMMARY.md`
</output>
