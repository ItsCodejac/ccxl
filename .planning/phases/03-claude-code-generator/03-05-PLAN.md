---
phase: 03-claude-code-generator
plan: 05
type: execute
---

<objective>
Create the CLAUDE.md generator (deep codebase-aware project context) and wire the full generator pipeline into the init command, producing all config files with preview and dry-run support.

Purpose: CLAUDE.md is the most visible config file — it's what Claude reads first. A good one saves developers from explaining their project every session. This plan also completes Phase 3 by wiring everything together.
Output: CLAUDE.md generator, full pipeline integration with init command, Phase 3 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-claude-code-generator/03-01-SUMMARY.md
@.planning/phases/03-claude-code-generator/03-02-SUMMARY.md
@.planning/phases/03-claude-code-generator/03-03-SUMMARY.md
@.planning/phases/03-claude-code-generator/03-04-SUMMARY.md
@src/generators/generator.ts
@src/generators/settings.ts
@src/commands/init.ts
@src/tui/views/InitView.tsx
@src/tui/components/FileList.tsx
@src/tui/components/PreviewPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE.md generator</name>
  <files>src/generators/claude-md.ts, src/generators/__tests__/claude-md.test.ts</files>
  <action>
  Create src/generators/claude-md.ts implementing Generator interface:

  Generates CLAUDE.md with sections based on ProjectAnalysis:

  1. Project Overview:
     - Project name, detected languages, frameworks
     - Package manager and monorepo info (if detected)

  2. Architecture:
     - Key dependencies listed with versions
     - Detected infrastructure (CI/CD, cloud, databases, Docker)
     - Framework-specific patterns (e.g., "This is a Next.js app with App Router" if next detected)

  3. Development:
     - Build/run/test commands inferred from package manager and detected tools
     - npm run dev / npm test / npm run build (or yarn/pnpm equivalents)
     - Framework-specific dev commands

  4. Standards:
     - Linting/formatting tools detected (eslint, prettier, etc.)
     - Test framework detected
     - "Follow existing patterns" with specific references to detected conventions

  5. Important Notes:
     - Database info and migration tool if detected
     - Docker setup if detected
     - CI/CD pipeline info if detected

  Do NOT generate boilerplate like "Always understand code before changes" — that's what the old ccxl did. Every line should be project-specific and derived from the analysis.

  Tests:
  - JS/TS React project → mentions React, includes npm commands
  - Python Django project → mentions Django, includes python/pip commands
  - Project with Prisma → mentions Prisma and migration commands
  - Verify no generic boilerplate lines
  - Verify CLAUDE.md is valid markdown

  Register in pipeline.
  </action>
  <verify>npm run test:run passes</verify>
  <done>CLAUDE.md generator produces project-specific context derived entirely from analysis, zero boilerplate</done>
</task>

<task type="auto">
  <name>Task 2: Wire full generator pipeline into init command</name>
  <files>src/generators/pipeline.ts, src/commands/init.ts, src/tui/views/GenerateView.tsx</files>
  <action>
  1. Create src/generators/pipeline.ts:
     - Import and register ALL generators: settings, skills, hooks, agents, mcp, claude-md
     - Export runPipeline(analysis: ProjectAnalysis, root: string): Promise<GeneratedFile[]>
     - Runs all generators in sequence, collects all files
     - Deduplicates by path (later generator wins)

  2. Create src/tui/views/GenerateView.tsx:
     - Receives GeneratedFile[] and renders:
       - FileList component showing all files to be created/modified
       - Count: "X files to generate"
       - ConfirmPrompt: "Write files?" (skip if --yes)
     - On confirm: writes all files using fs-extra
     - Shows success message with file count

  3. Update src/commands/init.ts:
     - After analysis display, run generator pipeline
     - For --dry-run: show FileList but don't write
     - For --yes: show summary and write immediately
     - For interactive: render GenerateView with confirm prompt
     - After writing: show "Done! Run `claude` to start using your new config"

  4. Also update the `generate` command subcommands to use individual generators:
     - ccxl generate settings → run settings generator only
     - ccxl generate skills → run skills generator only
     - ccxl generate all → run full pipeline
     - Other subcommands similarly mapped

  Verify end-to-end: `npm run build && node dist/cli.js init --dry-run` in ccxl's own directory should show all files that would be generated.
  </action>
  <verify>npm run build succeeds, node dist/cli.js init --dry-run shows file list, node dist/cli.js generate settings --dry-run shows settings.json content</verify>
  <done>Full pipeline wired to init and generate commands, dry-run shows all files, --yes writes them, Phase 3 complete</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:run` passes all tests
- [ ] `npm run build` succeeds
- [ ] `node dist/cli.js init --dry-run` shows analysis + file list
- [ ] `node dist/cli.js generate all --dry-run` shows all generated files
- [ ] Individual generators accessible via `ccxl generate <layer>`
- [ ] Phase 3 complete — all 5 plans done
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Full generator pipeline produces settings.json, skills, hooks, agents, .mcp.json, CLAUDE.md
- Init command works end-to-end with dry-run, --yes, and interactive modes
- Phase 3: Claude Code Generator is complete
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-code-generator/03-05-SUMMARY.md`
</output>
